<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.tech.futureteric.feedbacklibrary">

    <application>

        <activity android:name=".ui.FeedbackSystemActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <!-- TODO test parent activity up button is working the same in api 15 and 16 -->
        <activity android:name=".ui.AddFeatureRequestActivity"
            android:label="@string/label_create_request" />

    </application>

</manifest>